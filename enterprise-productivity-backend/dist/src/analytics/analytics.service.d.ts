import { NodePgDatabase } from 'drizzle-orm/node-postgres';
import { StreamService } from '../stream/stream.service';
import { AnalyticsScopeService } from './analytics-scope.service';
import type { AiDetail, AnalyticsOverview, ChannelsDetail, MessagesDetail, ModerationDetail, ResponseTimeDetail, StorageDetail, TeamsDetail, UsersDetail } from './analytics.types';
import type { AnalyticsDetailQueryDto, AnalyticsQueryDto } from './dto/analytics-query.dto';
export declare class AnalyticsService {
    private readonly db;
    private readonly streamService;
    private readonly scopeService;
    private readonly logger;
    private readonly cache;
    constructor(db: NodePgDatabase, streamService: StreamService, scopeService: AnalyticsScopeService);
    private cached;
    private resolveRange;
    private queryAllChannels;
    private getChannels;
    private resolveEffectiveChannelIds;
    private buildSearchFilter;
    private searchMessages;
    private aggregateMessages;
    private messageMetrics;
    private buildDailySeries;
    private storageNumbers;
    private aiCounts;
    private moderationNumbers;
    private pendingReports;
    private filterOptions;
    overview(username: string, query: AnalyticsQueryDto): Promise<AnalyticsOverview>;
    private storageDailySeries;
    private aiDailySeries;
    private moderationDailySeries;
    private buildDateSeries;
    messagesDetail(username: string, query: AnalyticsDetailQueryDto): Promise<MessagesDetail>;
    usersDetail(username: string, query: AnalyticsDetailQueryDto): Promise<UsersDetail>;
    channelsDetail(username: string, query: AnalyticsDetailQueryDto): Promise<ChannelsDetail>;
    teamsDetail(username: string, query: AnalyticsDetailQueryDto): Promise<TeamsDetail>;
    storageDetail(username: string, query: AnalyticsDetailQueryDto): Promise<StorageDetail>;
    aiDetail(username: string, query: AnalyticsDetailQueryDto): Promise<AiDetail>;
    moderationDetail(username: string, query: AnalyticsDetailQueryDto): Promise<ModerationDetail>;
    responseTimeDetail(username: string, query: AnalyticsDetailQueryDto): Promise<ResponseTimeDetail>;
    private serializeRange;
    private userNameMap;
}
