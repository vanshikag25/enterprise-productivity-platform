import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { NodePgDatabase } from 'drizzle-orm/node-postgres';
import { ConversationSummary } from '../database/schema/conversation-summaries.schema';
import { StreamService } from '../stream/stream.service';
import { ConversationSummaryProvider } from './conversation-summary.provider';
import { GenerateConversationSummaryDto } from './dto/generate-conversation-summary.dto';
export declare class ConversationSummaryService implements OnModuleInit, OnModuleDestroy {
    private readonly db;
    private readonly streamService;
    private readonly configService;
    private readonly provider;
    private readonly logger;
    private backfillTimer;
    constructor(db: NodePgDatabase, streamService: StreamService, configService: ConfigService, provider: ConversationSummaryProvider);
    onModuleInit(): void;
    onModuleDestroy(): void;
    private client;
    listSummaries(channelId: string, userId: string): Promise<{
        id: string;
        channelId: string;
        periodType: string;
        periodStart: Date;
        periodEnd: Date;
        overview: string;
        keyDecisions: string[];
        actionItems: string[];
        unresolvedTopics: string[];
        messageCount: number;
        provider: string;
        generatedAt: Date;
        createdAt: Date;
        updatedAt: Date;
    }[]>;
    getCurrent(channelId: string, userId: string, periodType: 'daily' | 'weekly'): Promise<ConversationSummary>;
    generate(channelId: string, userId: string, dto: GenerateConversationSummaryDto): Promise<ConversationSummary>;
    private generateManual;
    private buildAndStore;
    private findByPeriod;
    private runBackfill;
    private ensurePeriod;
    private allChannelIds;
    private describeChannel;
    private resolveChannel;
    private assertChannelMember;
    private fetchMessagesInPeriod;
    private fetchRecentMessages;
    private toMessage;
    private currentPeriod;
    private startOfUtcDay;
    private startOfUtcWeek;
    private addDays;
}
