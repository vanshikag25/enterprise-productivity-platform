import { NodePgDatabase } from 'drizzle-orm/node-postgres';
import type { Workflow, WorkflowActionResult } from '../database/schema/workflows.schema';
import { StreamService } from '../stream/stream.service';
import { NotificationsService } from '../notifications/notifications.service';
import { TasksService } from '../tasks/tasks.service';
import { RemindersService } from '../reminders/reminders.service';
import { ProjectMilestonesService } from '../project-milestones/project-milestones.service';
import { ConversationSummaryService } from '../conversation-summary/conversation-summary.service';
import { AiSummaryService } from '../ai-summary/ai-summary.service';
export declare class ActionExecutorService {
    private readonly db;
    private readonly streamService;
    private readonly notificationsService;
    private readonly tasksService;
    private readonly remindersService;
    private readonly milestonesService;
    private readonly conversationSummaryService;
    private readonly aiSummaryService;
    private readonly logger;
    constructor(db: NodePgDatabase, streamService: StreamService, notificationsService: NotificationsService, tasksService: TasksService, remindersService: RemindersService, milestonesService: ProjectMilestonesService, conversationSummaryService: ConversationSummaryService, aiSummaryService: AiSummaryService);
    executeAll(workflow: Workflow, payload: Record<string, unknown>): Promise<WorkflowActionResult[]>;
    private runAction;
    private notify;
    private chatMessage;
    private createTask;
    private createReminder;
    private updateTaskStatus;
    private updateMilestoneStatus;
    private aiSummary;
    private createTasksFromActionItems;
    private archiveDiscussion;
    private resolveChannelId;
    private resolveId;
    private resolveSingleUser;
    private resolveDate;
    private resolveUsers;
    private maybeNotify;
    private interpolate;
}
